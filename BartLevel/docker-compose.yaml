version: '2'
services:
    datascience-notebook:
        image: jupyter/base-notebook:latest
        volumes:
            - /tmp/jupyter_test_dir:/home/docker_worker/work            
        ports:
            - 8891:8888
        command: "start-notebook.sh"

    propio: 
        container_name: "ContenedorQueGeneraDatos"
        ports:
            - "8080:80"
        image: "nuestraImagen"